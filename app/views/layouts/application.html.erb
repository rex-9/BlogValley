<!DOCTYPE html>
<html>
  <head>
    <title>BlogApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%# <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <header>
    <nav class="flex">
      <% if user_signed_in? %>
        <%= link_to "Create Post", new_post_path, class: "button is-primary is-pulled-left mt-2 ml-5" %>
        <div class="username">
          <%= current_user.name %>
        </div>
        <%= button_to 'Sign Out', destroy_user_session_path, method: :delete, class: "button is-danger is-pulled-right mt-2 mr-5" %>
      <% end %>
    </nav>
  </header>
  <body>
    <div class="container">
      <% if alert.present? %>
        <div class="notification is-danger is-light">
          <%= alert %>
        </div>
      <% end %>
      <% if notice.present? %>
        <div class="notification is-link is-light mt-2">
          <%= notice %>
        </div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
