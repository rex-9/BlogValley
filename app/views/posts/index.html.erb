<%= render partial: "users/user", object: @user %>
<% @posts.each do |post| %>
  <%= render partial: "shared/post", object: post %>
  <%= render partial: "shared/comment", collection: post.recent_comments %>
<% end %>
<button class="button is-small">Pagination</button>
